export const filename = __filename
import { Doc } from '@frontarm/doc'

# Programmatic Navigation

There are times when you'll want some code to navigate the user to another location -- even if they haven't just clicked a `<Link>`. In cases like this, you'll need to call `push()` or `replace()` on your app's [history object](). And to get the history object, you can just call the `useHistory()` hook.

For example, here's how you could programmatically navigate the user after they've submitted a form:

```
// demo
```
